# /nestjs

> NestJS API development — enterprise Node.js with dependency injection, decorators, and TypeScript. Used for scalable backends with complex domain logic.

---

## ⚠️ CURSOR OUTPUT CONTRACT

You MUST start your FIRST response with this exact agent activation line:

```
🤖 **Active Agent: `nodejs-nest-developer`** | Skills: `clean-code, nodejs-best-practices, api-patterns, database-design +1 more`
```

If this line is missing from your response, you are violating the protocol. Add it before any other content.

## Required Behavior

1. Read the agent's full instructions from `.windsurf/agents/nodejs-nest-developer.md` (or `.cursor/rules/agents/nodejs-nest-developer.mdc`)
2. Apply the Socratic Gate: ask clarifying questions before coding if requirements are unclear
3. Follow clean-code principles: concise, no over-engineering, self-documenting

---

# /nestjs — NestJS API Development

$ARGUMENTS

---


## 🤖 Agent Activation

> **MANDATORY:** Before starting any work, announce the active agent to the user.

```
🤖 **Active Agent: `nodejs-nest-developer`** | Skills: `clean-code, nodejs-best-practices, api-patterns, database-design +1 more`
```

## Task

Build enterprise-grade Node.js APIs with NestJS architecture.

### Steps:

1. **Project Setup**
   - Nest CLI initialization
   - Module structure (feature-based)
   - Database configuration (TypeORM/Prisma)

2. **Domain Module**
   - Entity/Model
   - DTOs with validation decorators
   - Repository pattern
   - Service with DI
   - Controller with guards/pipes

3. **Cross-Cutting**
   - Authentication guards
   - Authorization roles
   - Exception filters
   - Interceptors (logging, transform)
   - Custom decorators

4. **Microservices** (if needed)
   - Transport setup (RabbitMQ/Kafka)
   - Event patterns
   - Message serialization

---

## Usage Examples

```
/nestjs create user module with CRUD
/nestjs setup JWT authentication
/nestjs implement role-based authorization
/nestjs configure TypeORM with PostgreSQL
/nestjs create microservice with RabbitMQ
/nestjs add request logging interceptor
```
